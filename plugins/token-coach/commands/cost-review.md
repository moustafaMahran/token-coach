---
description: Show a token-coach cost & efficiency review for the current session
---

Run the token-coach session report and present its output to the user **exactly as printed**, inside a code block. Do not summarise, re-interpret, or add analysis of your own — the script already formats everything.

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/tc-report.sh"
```

After showing the output, stop. Keep your own response to a single short sentence so this review itself stays cheap.
